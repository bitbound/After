@page
@model IndexModel
@{
    ViewData["Title"] = "Home";
}



@if (User.Identity.IsAuthenticated)
{
   <div class="row margin-top-lg">
       <div class="col-sm-6">
           <div class="h3">Characters</div>
           @if (Model.AfterUser.Characters?.Count > 0)
           {

           }
           else
           {
               <div class="margin-top-md">
                    No characters created.
               </div>
           }
           <div class="margin-top-md">
               <button class="btn btn-primary">Create New</button>
           </div>
       </div>


       <div class="col-sm-6">

       </div>
   </div>
}
else
{
    <link href="~/css/intro.css" rel="stylesheet" />

    <div class="row text-center margin-top-md">
        <div class="col-sm-12">
            <img id="imgTitle" src="~/Assets/Images/AfterTitle.png" />
        </div>
    </div>
    <div class="row text-center">
        <div id="divTunnel" class="col-sm-12">
            <img id="imgTunnel" src="~/Assets/Images/Tunnel and Shadow.png" class="glowing" />
            <div id="divParticles"></div>
        </div>
    </div>
    
    <div class="row">
        <div class="col-sm-12 text-center">
            <a href="~/Identity/Account/Login" class="btn btn-primary btn-lg login-button">
                Login
            </a>
            <br /><br />
            <a href="~/Identity/Account/Register" class="btn btn-default btn-lg login-button">
                Register
            </a>
        </div>
    </div>
    <div class="row">
        <div class="col-sm-12 footer text-right">
            Sound by <a href="http://www.freesound.org/people/ceich93/">Operation Contrast</a>.
        </div>
    </div>
}



@section Scripts {
    @if (User.Identity.IsAuthenticated)
    {
    }
    else
    {
        <script src="~/ts/Splash.js"></script>

    }
}